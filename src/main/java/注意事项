1，要给应用上下文命名，否则上传的文件会存入ROOT
2.刷新和局部刷新不会触发js相关事件，如change
3.ajax返回的是json格式串时，可能是表单默认提交造成的，input type要改为button
4.需要ajax响应时，servlet的响应类型要改为：
resp.setContentType("application/json;charset=UTF-8");
5.表单提交方式：使用 type="button" 的按钮不会提交表单，需要使用 type="submit" 的按钮或者在
JavaScript 中手动提交表单:document.getElementById('encryptrdForm').submit();。
6.后端重定向和json返回只需要一个
7.数据表更新时，model和DAO中的sql语句需要修改
8.文件路径更新时FileListServlet和ListFilesServlet需更新
9.getSession(false): 不创建新的会话，如果没有会话，返回 null。
  getSession(): 创建新会话（如果不存在会话），确保返回一个有效的会话对象。
10.数据库与类的属性要一一对应
11.有点面要素带内环需注意

2.常用方法：
Servlet:
request.getParameter("name(即表单中name的属性值)");
js:
为元素添加事件
element.addEventListener("事件类型",function);





遇到的报错：
重新部署会遇到该错误
1.Native library load failed.
java.lang.UnsatisfiedLinkError: Native Library D:\java\jre\bin\gdalalljni.dll
already loaded in another classloader